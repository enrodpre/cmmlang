message("Sourcing SourceDist.cmake...")

set(
  ${PROJECT_NAME}_SOURCE_DIST
  ON
)

# record the package version
set(
  GIT_TAG
  "@GIT_TAG@"
)
set(
  SEMVER
  "@SEMVER@"
)
set(
  VERSION
  "@VERSION@"
)
set(
  PROJECT_VERSION
  "@VERSION@"
)
set(
  VERSION_MAJOR
  "@VERSION_MAJOR@"
)
set(
  VERSION_MINOR
  "@VERSION_MINOR@"
)
set(
  VERSION_PATCH
  "@VERSION_PATCH@"
)
